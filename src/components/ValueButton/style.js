import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  valueButton: {
    display: "flex",
    justifyContent: "center",
    width: "25%",
    maxHeight: "100%",
    borderWidth: 1,
    aspectRatio: "1 / 1",
  },
  buttonText: {
    fontSize: 30,
    textAlign: "center",
  },
});
